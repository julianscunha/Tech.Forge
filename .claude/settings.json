{
  "permissions": {
    "allow": [
      "Edit",
      "Write",
      "Bash(npm test:*)",
      "Bash(pytest:*)"
    ]
  },
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "powershell -NoProfile -Command \"$input = [Console]::In.ReadLine() | ConvertFrom-Json; $fp = $input.tool_input.file_path; if ($fp -match '\\.py$') { ruff check --fix $fp } elseif ($fp -match '\\.(ts|tsx)$') { npx prettier --write $fp }\""
          }
        ]
      }
    ]
  }
}
