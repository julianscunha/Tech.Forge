{
  "permissions": {
    "allow": [
      "Edit",
      "Write",
      "Bash(npm test:*)",
      "Bash(pytest:*)"
    ]
  },
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "command": "if ($filePath -match '\\.py$') { ruff check --fix $filePath } elseif ($filePath -match '\\.(ts|tsx)$') { npx prettier --write $filePath }"
      }
    ]
  }
}
