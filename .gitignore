# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Node
node_modules/
.cache/
*.local

# Build output
dist/
graphify-out/
*.tsbuildinfo

# Fase 16 §10 — backend empacotado (PyInstaller, scripts/build-backend.ps1)
core/backend/dist-backend/
core/backend/build-backend/
core/backend/*.spec

# Env
.env
!.env.example

# Database
*.db
*.sqlite3

# Logs
logs/*.log
logs/*.txt
logs/*.jsonl
logs/pids/

# Runtime state (gerado, não versionar)
modules/installed/*/data/settings.json

# Fase 16 §33 — manifesto de integridade do Core (gerado por build/instalação local)
/core-integrity.json

# Módulos instalados via Marketplace/CLI — dado local do usuário, não do
# produto. hello_world/veeam_m365 são módulos de referência versionados
# (usados por testes e CI), todo o resto fica de fora do repositório.
/modules/
!/modules/installed/hello_world/

# Artefato acidental de rodar scripts/pytest com cwd=core/backend
# (settings.MODULES_INSTALLED_PATH resolve relativo ao CWD)
/core/backend/modules/
/cli/modules/

# IDE
.vscode/
.idea/
*.swp

# Agent tooling (config/skills locais — não fazem parte do produto)
.claude/
.agents/
.mcp.json
AGENTS.md
skills-lock.json

# Test/lint caches
.pytest_cache/
.ruff_cache/
