[pytest]
addopts = --strict-markers
markers =
    unit: fast, isolated, deterministic — sem rede/DB/TestClient (spec Fase 15 §5)
    integration: integra componentes reais (CliRunner + module_engine + filesystem) (spec Fase 15 §6)
